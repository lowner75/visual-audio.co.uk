//- views/work.pug

extends layouts/main

block variables
  - const bodyClass = 'work-page'
  - const bodyPage = 'work'

block content

  //- Work Page
  section
    .container.flex-center.justify-content-start(style="min-height: 100vh;")
      .d-block
        h1.mb-2(style="font-size: 10rem;font-weight: 300; letter-spacing: -0.05em;") Our Work
        .lead(style="font-size: 1.75rem; font-weight: 300; color: var(--text-muted);") Explore our portfolio of lighting, sound, and audio visual projects.