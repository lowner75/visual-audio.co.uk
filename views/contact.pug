//- views/contact.pug

extends layouts/main

block variables
  - const bodyClass = 'contact-page'
  - const bodyPage = 'contact'

block content

  //- Contact Page
  section
    .container.flex-center.justify-content-start(style="min-height: 100vh;")
      .d-block
        h1.mb-2(style="font-size: 10rem;font-weight: 300; letter-spacing: -0.05em;") Contact Us
        .lead(style="font-size: 1.75rem; font-weight: 300; color: var(--text-muted);") Get in touch with Visual Audio for all your lighting, sound, and audio visual needs.